# Camera settings
camera:
  device_id: 0
  width: 640
  height: 480
  fps_target: 30
  frame_skip: 2

# Detection settings
detection:
  model: "yolo26n_ncnn_256"
  detection_size: 256
  device: "cpu"
  confidence: 0.35
  iou: 0.4
  max_det: 10
  num_threads: 4

# Tracking settings
tracking:
  max_age: 30
  min_hits: 1
  iou_threshold: 0.25

# OCR settings
ocr:
  languages: ["en"]
  gpu: false
  min_confidence: 0.3

# Scene Captioning settings
captioning:
  model: "Salesforce/blip-image-captioning-base"
  device: "cpu"
  max_length: 30
  min_length: 5

# TTS settings
tts:
  rate: 200
  volume: 0.9

tracker:
  model: yolo26n.pt
  confidence: 0.55
  iou_threshold: 0.45
  max_age: 30
  min_hits: 3
  device: cpu

warnings:
  enabled: true
  global_cooldown: 3.0
  per_track_cooldown: 4.0
  min_confidence: 0.65
  min_bbox_ratio: 0.02
  max_bbox_ratio: 0.35
  risk_classes:
    - person
    - bicycle
    - car
    - motorcycle
    - bus
    - truck

# Logging
logging:
  level: "INFO"
  show_fps: true
